# yaml-language-server: $schema=panopticon.schema.json
commands:
  - cmd: go build
    watch_paths:
      - ./
    ignore_paths:
      - .git
      - .github
  - cmd: go test ./...
    watch_paths:
      - ./
    ignore_paths:
      - .git
      - .github
